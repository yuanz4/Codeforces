#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	ios_base::sync_with_stdio(false);
    cin.tie(0);
	int n, k;
	cin >> n >> k;
	int p[n];
	for (int i = 0; i < n; i++)
		cin >> p[i];
	int x[k], y[k];
	for (int i = 0; i < k; i++)
		cin >> x[i] >> y[i];
	
	return 0;
}