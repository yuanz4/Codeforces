#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {
	ios_base::sync_with_stdio(false);
    cin.tie(0);
	int test;
	cin >> test;
	for (int t = 1; t <= test; t++) {
		cout << "Case #" << t << ": ";
		int r, c, f, s;
		cin >> r >> c >> f >> s;
		string a[r];
		for (int i = 0; i < r; i++)
			cin >> a[i];
		string b[r];
		for (int i = 0; i < r; i++)
			cin >> b[i];

	}
	return 0;
}